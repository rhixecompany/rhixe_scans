# rhixe_scans — Bun configuration
# Next.js comic reader platform (Prisma + Stripe/PayPal)

[build]
loader = false

[test]
coverage = true
root = "./src"

[install]
optional = true
dev = true
peer = true
